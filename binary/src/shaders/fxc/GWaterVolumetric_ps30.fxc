sampler DEPTH 	: register(s0);
float3 SCR_S	: register(c0);
float RADIUS 	: register(c1);

struct PS_INPUT {
	float2 P 		: VPOS;
	float2 coord	: TEXCOORD0;
};

float4 main(PS_INPUT i) : COLOR {
	float4 depth = tex2D(DEPTH, i.P / SCR_S.xy);
	return float4(SCR_S, SCR_S.z);
};